/* Totus Central · UI compacta de relojes
   La cabecera informa; las acciones se realizan en ventanas emergentes. */
(function(){
  let sig='';
  const att=()=>typeof openAttendance==='function'?openAttendance():null;
  const timer=()=>typeof openTaskTime==='function'?openTaskTime():null;
  const intr=()=>typeof openTaskInterruption==='function'?openTaskInterruption():null;
  const brk=()=>typeof openPersonalBreak==='function'?openPersonalBreak():null;
  const task=()=>{const x=timer()||intr(),id=x?.task_id;return id?(db.tasks||[]).find(t=>t.id===id):null};
  const openTasks=()=>{const uid=me()?.id;return(db.tasks||[]).filter(t=>t.assigned_to===uid&&!['done','cancelled'].includes(t.status))};
  const time=x=>x?fmtDur(elapsedSec(x.started_at||x.clock_in)):'00:00:00';
  const workLoc=x=>x?.location_id||(task()?.location_id)||att()?.location_id||'';
  function signature(){const a=att(),t=timer(),i=intr(),b=brk(),w=task();return[a?.id||'',t?.id||'',i?.id||'',b?.id||'',w?.id||'',w?.title||'',openTasks().length].join('|')}
  function render(force=false){const host=document.getElementById('shellWorkbar');if(!host||!me())return;const s=signature();if(!force&&s===sig&&host.querySelector('.clock-chip'))return;sig=s;const a=att(),t=timer(),i=intr(),b=brk(),w=task(),stand=i&&!i.task_id,workLabel=i?'LABORAL':t?'TAREA':'TRABAJO',workTitle=stand?interruptionName(i.reason_code):w?.title||'Sin tarea activa',workSub=stand?`${locationName(workLoc(i))} · computa`:i&&w?`${interruptionName(i.reason_code)} · computa`:w?locationName(w.location_id):openTasks().length?`${openTasks().length} pendiente${openTasks().length===1?'':'s'}`:'Sin tareas pendientes';host.innerHTML=`<button class="clock-chip clock-chip-journey ${a?'active':''}" onclick="clockOpenJourney()"><span>JORNADA</span><strong id="compactAttendanceTime">${a?time(a):'00:00:00'}</strong><small>${esc(a?locationName(a.location_id):'Sin jornada')}</small></button><button class="clock-chip clock-chip-work ${t||i?'active':''}" onclick="clockOpenTask()"><div><span>${workLabel}</span><strong id="compactTaskTime">${t?time(t):i?time(i):'00:00:00'}</strong></div><b>${esc(workTitle)}</b><small>${esc(workSub)}</small></button><button class="clock-chip clock-chip-break ${b?'active':''}" onclick="clockOpenBreak()"><span>PARADA</span><strong id="compactBreakTime">${b?time(b):'00:00:00'}</strong><small>${esc(b?breakName(b.break_type):'Sin parada')}</small></button><button class="clock-control-button" onclick="clockOpenHub()"><span>Control</span><b>•••</b></button>`}
  function refresh(){const set=(id,v)=>{const e=document.getElementById(id);if(e)e.textContent=v},a=att(),t=timer(),i=intr(),b=brk();set('compactAttendanceTime',a?time(a):'00:00:00');set('compactTaskTime',t?time(t):i?time(i):'00:00:00');set('compactBreakTime',b?time(b):'00:00:00');if(signature()!==sig||!document.querySelector('#shellWorkbar .clock-chip'))render(true)}
  function status(label,value,detail,tone=''){return `<div class="clock-modal-status ${tone}"><small>${esc(label)}</small><strong>${esc(value)}</strong><span>${esc(detail||'')}</span></div>`}
  window.clockOpenHub=function(){const a=att(),t=timer(),i=intr(),b=brk(),w=task(),stand=i&&!i.task_id,work=stand?interruptionName(i.reason_code):w?.title||'Sin tarea activa';modal(`<div class="section-head"><div><div class="eyebrow">Control diario</div><h3>Jornada y trabajo</h3><div class="small">Consulta el estado y entra solo en la acción que necesitas.</div></div><button class="ghost" onclick="closeModal()">Cerrar</button></div><div class="clock-hub-grid"><button class="clock-hub-card ${a?'active':''}" onclick="closeModal();clockOpenJourney()"><small>JORNADA</small><b>${a?time(a):'Sin jornada'}</b><span>${a?esc(locationName(a.location_id)):'Entrada y salida'}</span></button><button class="clock-hub-card ${t||i?'active work':''}" onclick="closeModal();clockOpenTask()"><small>${i?'ACTIVIDAD LABORAL':'TAREA'}</small><b>${esc(work)}</b><span>${t?time(t):i?time(i):`${openTasks().length} pendientes`}</span></button><button class="clock-hub-card ${b?'active personal':''}" onclick="closeModal();clockOpenBreak()"><small>PARADA PERSONAL · NO COMPUTA</small><b>${b?esc(breakName(b.break_type)):'Sin parada'}</b><span>${b?time(b):'Cigarro, llamada, recado…'}</span></button><button class="clock-hub-card work-action" onclick="closeModal();clockWorkMenu()" ${a&&!b&&!i?'':'disabled'}><small>ACTIVIDAD LABORAL · COMPUTA</small><b>Registrar actividad</b><span>Cliente, proveedor, reunión, llamada o incidencia.</span></button></div><div class="clock-hub-footer"><button class="ghost" onclick="closeModal();clockHelp()">Ayuda de relojes</button></div>`)};
  window.clockOpenJourney=function(){const a=att();if(a)return modal(`<div class="section-head"><div><div class="eyebrow">Jornada</div><h3>Jornada en curso</h3><div class="small">${esc(locationName(a.location_id))} · entrada ${dt(a.clock_in)}</div></div><button class="ghost" onclick="closeModal()">Cerrar</button></div>${status('TIEMPO DE JORNADA',time(a),locationName(a.location_id),'journey')}<div class="actions clock-modal-actions"><button class="danger" onclick="closeModal();clockAttendanceToggle()">Fichar salida</button><button class="secondary" onclick="closeModal();goRoot('tasks','attendance')">Ver fichajes</button></div>`);const loc=(db.locations||[]).find(x=>x.active!==false)?.id||'';modal(`<div class="section-head"><div><div class="eyebrow">Jornada</div><h3>Fichar entrada</h3><div class="small">Selecciona dónde empiezas a trabajar.</div></div><button class="ghost" onclick="closeModal()">Cerrar</button></div><div class="clock-quick-form"><label>Centro o proyecto</label><select id="clockLocation">${(db.locations||[]).filter(x=>x.active!==false).map(x=>`<option value="${x.id}" ${x.id===loc?'selected':''}>${esc(x.name)}</option>`).join('')}</select><div class="actions"><button class="primary" onclick="clockAttendanceToggle()">Entrar y comenzar jornada</button></div></div>`)};
  window.clockOpenTask=function(){const a=att(),t=timer(),i=intr(),w=task(),stand=i&&!i.task_id;if(stand)return modal(`<div class="section-head"><div><div class="eyebrow">Actividad laboral</div><h3>${esc(interruptionName(i.reason_code))}</h3><div class="small">Actividad independiente de una tarea · COMPUTA.</div></div><button class="ghost" onclick="closeModal()">Cerrar</button></div>${status('TIEMPO COMPUTADO',time(i),locationName(workLoc(i)),'work')}<div class="actions clock-modal-actions"><button class="primary" onclick="closeModal();clockStopWork()">Finalizar actividad</button></div>`);if(i&&w)return modal(`<div class="section-head"><div><div class="eyebrow">Tarea suspendida</div><h3>${esc(w.title)}</h3><div class="small">${esc(interruptionName(i.reason_code))} · COMPUTA.</div></div><button class="ghost" onclick="closeModal()">Cerrar</button></div>${status('ACTIVIDAD LABORAL',time(i),locationName(w.location_id),'work')}<div class="actions clock-modal-actions"><button class="primary" onclick="closeModal();clockResumeTask('${w.id}')">Reanudar tarea</button><button class="secondary" onclick="closeModal();openTaskCard('${w.id}')">Abrir tarea</button></div>`);if(t&&w)return modal(`<div class="section-head"><div><div class="eyebrow">Tarea en marcha</div><h3>${esc(w.title)}</h3><div class="small">${esc(locationName(w.location_id))}</div></div><button class="ghost" onclick="closeModal()">Cerrar</button></div>${status('TIEMPO EN TAREA',time(t),'El contador sigue activo.','work')}<div class="clock-task-primary"><button class="primary" onclick="closeModal();clockPauseTask()">Pausar tarea</button><button class="secondary" onclick="closeModal();openActiveTask()">Abrir ficha</button><button class="secondary" onclick="closeModal();clockTaskPicker()">Cambiar tarea</button></div><div class="clock-modal-separator"></div><button class="clock-inline-action" onclick="closeModal();clockWorkMenu()"><b>Registrar actividad laboral</b><span>Pausa la tarea y sigue computando trabajo por cliente, proveedor, reunión o incidencia.</span></button>`);if(!a)return modal(`<div class="section-head"><div><div class="eyebrow">Tarea</div><h3>Primero inicia tu jornada</h3><div class="small">No se contabiliza una tarea sin jornada abierta.</div></div><button class="ghost" onclick="closeModal()">Cerrar</button></div><div class="actions"><button class="primary" onclick="closeModal();clockOpenJourney()">Fichar entrada</button></div>`);clockTaskPicker()};
  window.clockOpenBreak=function(){const b=brk();if(b)return modal(`<div class="section-head"><div><div class="eyebrow">Parada personal</div><h3>${esc(breakName(b.break_type))}</h3><div class="small">Este tiempo NO COMPUTA.</div></div><button class="ghost" onclick="closeModal()">Cerrar</button></div>${status('TIEMPO DE PARADA',time(b),'No computable','personal')}<div class="actions clock-modal-actions"><button class="primary" onclick="closeModal();clockStopPersonal()">Finalizar parada</button></div>`);clockPersonalMenu()};
  const obs=new MutationObserver(()=>{if(!document.querySelector('#shellWorkbar .clock-chip'))requestAnimationFrame(()=>render(true))});
  const host=()=>document.getElementById('shellWorkbar');requestAnimationFrame(()=>{if(host()){obs.observe(host(),{childList:true});render(true)};setInterval(refresh,1000)});
})();
