import {esc,eur,pct,duration,fmtDate,fmtDateTime,humanStatus,priorityLabel,badge,badgeClass,discountRows,pricingMetrics,pricingRealCost,naturalDays,monthKey,monthName,monthDays,isoDay,option,can} from './core.js';
const e=esc;
export const field=(label,name,value='',type='text',extra='')=>`<label>${e(label)}<input name="${e(name)}" type="${type}" value="${e(value??'')}" ${extra}></label>`;
export const textarea=(label,name,value='',extra='')=>`<label>${e(label)}<textarea name="${e(name)}" ${extra}>${e(value??'')}</textarea></label>`;
export const select=(label,name,items,current='',extra='')=>`<label>${e(label)}<select name="${e(name)}" ${extra}>${items.map(x=>option(x[0],x[1],current)).join('')}</select></label>`;
export const empty=(title,text='')=>`<div class="empty"><strong>${e(title)}</strong>${text?`<span>${e(text)}</span>`:''}</div>`;
const person=(id,ctx)=>ctx.base.members.find(x=>x.id===id)?.name||'—';
const loc=(id,ctx)=>ctx.base.locations.find(x=>x.id===id)?.name||'—';
const modal=(title,body,wide=false)=>`<div class="modal-backdrop" data-close-modal><section class="modal ${wide?'wide':''}" role="dialog" aria-modal="true"><header><h2>${e(title)}</h2><button class="icon-button" type="button" data-close-modal>✕</button></header><div class="modal-body">${body}</div></section></div>`;
export const renderModal=modal;
export function panel(ctx){const d=ctx.panel||{},open=d.attendance;return `<div class="employee-card"><div class="avatar large">${e((ctx.me.name||'?')[0])}</div><div class="grow"><div class="eyebrow">Mi jornada</div><h3>${e(ctx.me.name)}</h3><p>${open?'Entrada '+fmtDateTime(open.clock_in_at)+' · '+loc(open.location_id,ctx):'Sin jornada abierta'}</p></div><div class="actions"><button class="${open?'danger':'primary'}" data-action="attendance-toggle">${open?'Salir':'Entrar'}</button>${open?'<button class="secondary" data-action="break-toggle">'+(d.break?'Terminar pausa':'Pausa personal')+'</button>':''}</div></div><div class="metric-grid"><div class="metric"><span>Tareas pendientes</span><b>${d.tasks?.length||0}</b></div><div class="metric"><span>Avisos activos</span><b>${d.notices?.length||0}</b></div><div class="metric"><span>Jornada</span><b>${open?'Activa':'Cerrada'}</b></div><div class="metric"><span>Pausa</span><b>${d.break?'Activa':'No'}</b></div><div class="metric"><span>Tarea en curso</span><b>${d.timer?'Sí':'No'}</b></div><div class="metric"><span>Rol</span><b>${e(ctx.roleLabel)}</b></div></div><div class="grid two"><section class="card"><div class="card-head"><div><div class="eyebrow">Trabajo</div><h3>Mis tareas</h3></div></div>${(d.tasks||[]).slice(0,6).map(t=>`<div class="list-row"><div><strong>${e(t.title)}</strong><span>${e(priorityLabel(t.priority))} · ${t.due_at?fmtDateTime(t.due_at):'sin fecha'}</span></div>${badge(humanStatus(t.status))}</div>`).join('')||empty('Sin tareas pendientes')}</section><section class="card"><div class="card-head"><div><div class="eyebrow">Avisos</div><h3>Notificaciones</h3></div>${can(ctx.me.role,'gerente')?'<button class="mini" data-action="notice-new">Nuevo aviso</button>':''}</div>${(d.notices||[]).map(n=>`<div class="list-row"><div><strong>${e(n.title)}</strong><span>${e(n.message)}</span></div><div class="row-actions">${n.ack_at?badge('Confirmado','success'):`<button class="mini" data-action="notice-read" data-id="${n.id}" data-ack="${n.requires_ack?'1':'0'}">${n.requires_ack?'Confirmar':'Leído'}</button>`}</div></div>`).join('')||empty('Sin avisos activos')}</section></div>`}
export function calendar(ctx){const ym=ctx.month||monthKey(),days=monthDays(ym),events=ctx.events||[];if(ctx.sub==='agenda'){return `<section class="card"><div class="card-head"><div><div class="eyebrow">Agenda</div><h3>${e(monthName(ym))}</h3></div></div>${events.sort((a,b)=>String(a.date).localeCompare(String(b.date))).map(x=>`<div class="list-row"><div><strong>${fmtDate(x.date)} · ${e(x.title)}</strong><span>${e(x.type)}</span></div>${x.status?badge(humanStatus(x.status)):''}</div>`).join('')||empty('Sin elementos planificados')}</section>`}const first=new Date(`${ym}-01T12:00:00`).getDay()||7,cells=[];for(let i=1;i<first;i++)cells.push('<div class="cal-cell muted"></div>');for(let d=1;d<=days;d++){const date=isoDay(ym,d),ev=events.filter(x=>x.date===date);cells.push(`<div class="cal-cell"><b>${d}</b>${ev.slice(0,4).map(x=>`<span class="cal-event ${e(x.kind||'task')}">${e(x.title)}</span>`).join('')}</div>`)}return `<section class="card"><div class="card-head"><div><div class="eyebrow">Calendario</div><h3>${e(monthName(ym))}</h3></div><div class="actions"><button class="mini" data-month="prev">‹</button><button class="mini" data-month="next">›</button></div></div><div class="calendar-weekdays"><span>L</span><span>M</span><span>X</span><span>J</span><span>V</span><span>S</span><span>D</span></div><div class="calendar-grid">${cells.join('')}</div></section>`}
export function tasks(ctx){const hist=ctx.sub==='history',rows=(ctx.tasks||[]).filter(t=>hist?t.status==='completed':t.status!=='completed');return `<div class="toolbar"><div class="search"><input data-filter="tasks" placeholder="Buscar tarea"></div>${can(ctx.me.role,'encargado')?'<button class="primary" data-action="task-new">Nueva tarea</button>':''}</div><section class="card table-card"><div class="table-scroll"><table><thead><tr><th>Tarea</th><th>Asignada</th><th>Tienda</th><th>Prioridad</th><th>Fecha</th><th>Estado</th><th></th></tr></thead><tbody>${rows.map(t=>`<tr><td><strong>${e(t.title)}</strong><span class="subline">${e(t.description||'')}</span></td><td>${e(person(t.assigned_to,ctx))}</td><td>${e(loc(t.location_id,ctx))}</td><td>${badge(priorityLabel(t.priority),badgeClass(t.priority))}</td><td>${t.due_at?fmtDateTime(t.due_at):'—'}</td><td>${badge(humanStatus(t.status))}</td><td><div class="row-actions">${!hist?`<button class="mini" data-action="task-open" data-id="${t.id}">Abrir</button>`:''}${can(ctx.me.role,'encargado')?`<button class="mini" data-action="task-edit" data-id="${t.id}">Editar</button>`:''}</div></td></tr>`).join('')}</tbody></table></div>${rows.length?'':empty(hist?'Sin tareas terminadas':'Sin tareas abiertas')}</section>`}
export function hours(ctx){return `<div class="metric-grid three"><div class="metric"><span>Jornadas</span><b>${ctx.attendance?.length||0}</b></div><div class="metric"><span>Pausas personales</span><b>${ctx.breaks?.length||0}</b></div><div class="metric"><span>Interrupciones</span><b>${ctx.interruptions?.length||0}</b></div></div><section class="card table-card"><div class="table-scroll"><table><thead><tr><th>Empleado</th><th>Entrada</th><th>Salida</th><th>Tienda</th><th>Duración</th></tr></thead><tbody>${(ctx.attendance||[]).map(x=>`<tr><td>${e(person(x.member_id,ctx))}</td><td>${fmtDateTime(x.clock_in_at)}</td><td>${x.clock_out_at?fmtDateTime(x.clock_out_at):badge('Abierta','info')}</td><td>${e(loc(x.location_id,ctx))}</td><td>${x.clock_out_at?duration((new Date(x.clock_out_at)-new Date(x.clock_in_at))/60000):'—'}</td></tr>`).join('')}</tbody></table></div></section>`}
const weekday=['Lunes','Martes','Miércoles','Jueves','Viernes','Sábado','Domingo'];
export function schedule(ctx){if(ctx.sub==='store'){return `<div class="toolbar">${can(ctx.me.role,'gerente')?'<button class="primary" data-action="opening-edit">Editar horario de tienda</button><button class="secondary" data-action="opening-ex-new">Nueva excepción</button>':''}</div>${ctx.base.locations.map(l=>`<section class="card"><div class="card-head"><div><div class="eyebrow">Tienda</div><h3>${e(l.name)}</h3></div></div>${weekday.map((w,i)=>{const r=(ctx.opening||[]).find(x=>x.location_id===l.id&&x.weekday===i+1);return `<div class="list-row"><div><strong>${w}</strong><span>${!r||r.closed?'Cerrado':`${r.start_time?.slice(0,5)||''} – ${r.end_time?.slice(0,5)||''}${r.split_start_time?` / ${r.split_start_time.slice(0,5)} – ${r.split_end_time?.slice(0,5)||''}`:''}`}</span></div></div>`}).join('')}</section>`).join('')}`}
if(ctx.sub==='exceptions')return `<div class="grid two"><section class="card"><div class="card-head"><h3>Excepciones de empleados</h3>${can(ctx.me.role,'gerente')?'<button class="mini" data-action="schedule-ex-new">Añadir</button>':''}</div>${(ctx.scheduleEx||[]).map(x=>`<div class="list-row"><div><strong>${fmtDate(x.exception_date)} · ${e(person(x.member_id,ctx))}</strong><span>${x.off?'Libre':`${x.start_time?.slice(0,5)} – ${x.end_time?.slice(0,5)}`} · ${e(x.reason||'')}</span></div></div>`).join('')||empty('Sin excepciones')}</section><section class="card"><div class="card-head"><h3>Excepciones de tienda</h3>${can(ctx.me.role,'gerente')?'<button class="mini" data-action="opening-ex-new">Añadir</button>':''}</div>${(ctx.openingEx||[]).map(x=>`<div class="list-row"><div><strong>${fmtDate(x.exception_date)} · ${e(loc(x.location_id,ctx))}</strong><span>${x.closed?'Cerrado':`${x.start_time?.slice(0,5)} – ${x.end_time?.slice(0,5)}`} · ${e(x.reason||'')}</span></div></div>`).join('')||empty('Sin excepciones')}</section></div>`;
return `<div class="toolbar">${can(ctx.me.role,'gerente')?'<button class="primary" data-action="schedule-edit">Editar horario empleado</button>':''}</div>${ctx.base.members.filter(x=>x.active).map(m=>`<section class="card"><div class="card-head"><div><div class="eyebrow">Empleado</div><h3>${e(m.name)}</h3></div></div>${weekday.map((w,i)=>{const r=(ctx.schedule||[]).find(x=>x.member_id===m.id&&x.weekday===i+1);return `<div class="list-row"><div><strong>${w}</strong><span>${!r||r.off?'Libre':`${r.start_time?.slice(0,5)||''} – ${r.end_time?.slice(0,5)||''}${r.split_start_time?` / ${r.split_start_time.slice(0,5)} – ${r.split_end_time?.slice(0,5)||''}`:''}`}</span></div><span>${r?.location_id?e(loc(r.location_id,ctx)):''}</span></div>`}).join('')}</section>`).join('')}`}
export function leave(ctx){if(ctx.sub==='calendar')return calendar({...ctx,events:(ctx.periods||[]).map(p=>({date:p.from_date,title:`${person(p.member_id,ctx)} · ${p.leave_type}`,kind:'leave',type:'Ausencia'})).concat((ctx.holidays||[]).filter(h=>h.active).map(h=>({date:h.holiday_date,title:h.name,kind:'holiday',type:'Festivo'})))});return `<div class="toolbar"><button class="primary" data-action="leave-request">Solicitar ausencia</button>${can(ctx.me.role,'gerente')?'<button class="secondary" data-action="gift-new">Regalar día</button><button class="secondary" data-action="holiday-new">Añadir festivo</button>':''}</div><div class="grid two"><section class="card"><div class="card-head"><h3>Solicitudes</h3></div>${(ctx.leaveReq||[]).map(r=>`<div class="list-row"><div><strong>${e(person(r.member_id,ctx))} · ${e(r.leave_type)}</strong><span>${fmtDate(r.from_date)} – ${fmtDate(r.to_date)} · ${r.natural_days||naturalDays(r.from_date,r.to_date)} días · ${e(r.reason||'')}</span></div><div class="row-actions">${badge(humanStatus(r.status))}${can(ctx.me.role,'gerente')&&r.status==='pending'?`<button class="mini primary" data-action="leave-resolve" data-id="${r.id}" data-status="approved">Aprobar</button><button class="mini" data-action="leave-resolve" data-id="${r.id}" data-status="rejected">Rechazar</button>`:''}</div></div>`).join('')||empty('Sin solicitudes')}</section><section class="card"><h3>Festivos</h3>${(ctx.holidays||[]).filter(h=>h.active).map(h=>`<div class="list-row"><div><strong>${fmtDate(h.holiday_date)} · ${e(h.name)}</strong><span>${h.location_id?e(loc(h.location_id,ctx)):'General'}</span></div></div>`).join('')||empty('Sin festivos')}</section></div>`}
export function purchases(ctx){const hist=ctx.sub==='history',rows=(ctx.purchases||[]).filter(x=>hist?['delivered','cancelled','rejected'].includes(x.status):!['delivered','cancelled','rejected'].includes(x.status));return `<div class="toolbar"><button class="primary" data-action="purchase-new">Nueva compra</button></div><section class="card table-card"><div class="table-scroll"><table><thead><tr><th>Empleado</th><th>Producto</th><th>Tipo</th><th>Cantidad</th><th>Precio/u</th><th>Total</th><th>Estado</th><th></th></tr></thead><tbody>${rows.map(x=>`<tr><td>${e(person(x.member_id,ctx))}</td><td><strong>${e(x.product_name)}</strong><span class="subline">${e(x.notes||'')}</span></td><td>${x.mode==='store'?'Tienda':'Proveedor'}</td><td>${x.quantity}</td><td>${eur(x.unit_price)}</td><td>${eur(x.total)}</td><td>${badge(humanStatus(x.status))}</td><td>${can(ctx.me.role,'gerente')&&!hist?`<button class="mini" data-action="purchase-status" data-id="${x.id}">Estado</button>`:''}</td></tr>`).join('')}</tbody></table></div>${rows.length?'':empty(hist?'Sin histórico':'Sin compras activas')}</section>`}
export function pricing(ctx){if(ctx.sub==='consultations')return `<div class="toolbar"><button class="primary" data-action="pricing-quick">Nueva consulta</button></div><section class="card">${(ctx.consultations||[]).map(c=>`<div class="list-row"><div><strong>${e(c.name)}</strong><span>Coste ${eur(c.net_cost)} · PVP ${eur(c.pvp)} · ${fmtDateTime(c.created_at)}</span></div><div class="row-actions">${badge(humanStatus(c.status))}<button class="mini" data-action="consultation-open" data-id="${c.id}">Reabrir</button>${!c.linked_product_id?`<button class="mini" data-action="consultation-convert" data-id="${c.id}">Convertir</button>`:''}</div></div>`).join('')||empty('Sin consultas')}</section>`;
if(ctx.sub==='catalog')return `<div class="toolbar"><input class="search" data-filter="products" placeholder="Buscar SKU o producto"><button class="primary" data-action="product-new">Nuevo producto</button></div><div class="catalog-grid">${(ctx.products||[]).map(p=>`<section class="card product-card"><div class="eyebrow">${e(p.sku)}</div><h3>${e(p.name)}</h3><p>${e(ctx.brandMap?.[p.brand_id]||'Sin marca')} · ${e(ctx.familyMap?.[p.family_id]||'Sin familia')}</p><div class="status-strip">${badge(humanStatus(p.status))}<span class="badge info">${eur(p.pvp)}</span></div><button class="secondary" data-action="product-open" data-id="${p.id}">Abrir ficha</button></section>`).join('')||empty('Catálogo vacío')}</div>`;
return `<div class="pricing-hero"><div><div class="eyebrow">Totus Pricing</div><h2>Consulta rápida</h2><p>Coste real, PVP, margen, mercado y descuentos.</p></div><button class="secondary" data-action="pricing-settings">Ajustes</button></div><div class="grid pricing-layout"><section class="card"><form id="pricingQuickForm" class="form-stack"><div class="form-grid two">${field('Nombre','name',ctx.draft?.name||'')}${field('Coste neto','net_cost',ctx.draft?.net_cost||'','text','inputmode="decimal"')}${field('PVP propuesto','pvp',ctx.draft?.pvp||'','text','inputmode="decimal"')}${field('Competencia','competitor_price',ctx.draft?.competitor_price||'','text','inputmode="decimal"')}</div>${textarea('Notas','notes',ctx.draft?.notes||'')}<div class="actions"><button class="primary" type="submit">Guardar consulta</button><button class="secondary" type="button" data-action="pricing-clear">Limpiar</button></div></form></section><section class="card result-panel" id="pricingResult">${pricingResult(ctx.draft||{},ctx.settings||{})}</section></div>`}
export function pricingResult(draft,settings){const cost=pricingRealCost({net_cost:draft.net_cost||0}),m=pricingMetrics({cost,pvp:draft.pvp,competitors:draft.competitor_price?[{price:draft.competitor_price}]:[]},settings),rows=discountRows(draft.pvp,cost,settings);return `<div class="eyebrow">Resultado</div><h3>${m.sale?e(m.marginState):'Introduce cifras'}</h3><div class="metric-grid two"><div class="metric"><span>Coste real</span><b>${eur(cost)}</b></div><div class="metric"><span>Margen €/u</span><b>${eur(m.margin)}</b></div><div class="metric"><span>Margen s/PVP</span><b>${pct(m.marginPct)}</b></div><div class="metric"><span>Mercado</span><b>${e(m.market)}</b></div></div><div class="table-scroll"><table><thead><tr><th>Dto.</th><th>Precio</th><th>Margen</th><th>Estado</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${r.discount}%</td><td>${eur(r.price)}</td><td>${pct(r.marginPct)}</td><td>${badge(r.status)}</td></tr>`).join('')}</tbody></table></div>`}
export function library(ctx){if(ctx.sub==='questions')return `<div class="toolbar"><button class="primary" data-action="question-new">Nueva consulta</button></div><section class="card">${(ctx.questions||[]).map(q=>`<div class="list-row"><div><strong>${e(person(q.member_id,ctx))}: ${e(q.question)}</strong>${q.answer?`<span>Respuesta: ${e(q.answer)}</span>`:'<span>Pendiente de respuesta</span>'}</div><div class="row-actions">${badge(humanStatus(q.status))}${can(ctx.me.role,'gerente')&&q.status!=='resolved'?`<button class="mini" data-action="question-answer" data-id="${q.id}">Responder</button>`:''}</div></div>`).join('')||empty('Sin consultas')}</section>`;return `<div class="toolbar">${can(ctx.me.role,'gerente')?'<button class="primary" data-action="library-upload">Subir documento</button>':''}</div><div class="catalog-grid">${(ctx.library||[]).map(d=>`<section class="card doc-card"><div><div class="eyebrow">${e(d.category||'Documento')}</div><h3>${e(d.title)}</h3><p>${e(d.notes||d.file_name||'')}</p></div><button class="secondary" data-action="file-download" data-bucket="internal-library" data-path="${e(d.storage_path)}">Descargar</button></section>`).join('')||empty('Biblioteca vacía')}</div>`}
export function people(ctx){if(ctx.sub==='documents')return `<div class="toolbar"><button class="primary" data-action="document-upload">Subir documento</button></div><section class="card">${(ctx.documents||[]).map(d=>`<div class="list-row"><div><strong>${e(person(d.member_id,ctx))} · ${e(d.title)}</strong><span>${e(d.category||'')} · ${e(d.notes||'')}</span></div><button class="mini" data-action="file-download" data-bucket="employee-documents" data-path="${e(d.storage_path)}">Descargar</button></div>`).join('')||empty('Sin documentación')}</section>`;if(ctx.sub==='incidents')return `<div class="toolbar"><button class="primary" data-action="incident-new">Nueva incidencia</button></div><section class="card">${(ctx.incidents||[]).map(i=>`<div class="list-row"><div><strong>${e(person(i.member_id,ctx))} · ${e(i.title)}</strong><span>${e(i.description)} · ${fmtDateTime(i.created_at)}</span></div>${badge(humanStatus(i.status))}</div>`).join('')||empty('Sin incidencias')}</section>`;return `<div class="toolbar">${ctx.me.role==='admin'?'<button class="primary" data-action="member-new">Nuevo usuario</button>':''}</div><div class="user-grid">${ctx.base.members.map(m=>`<section class="card user-card"><div class="avatar large">${e((m.name||'?')[0])}</div><div class="grow"><div class="eyebrow">${e(m.role)}</div><h3>${e(m.name)}</h3><p>${e(m.email)}${m.phone?` · ${e(m.phone)}`:''}</p><div class="status-strip">${badge(m.active?'Activo':'Inactivo',m.active?'success':'danger')}</div></div>${ctx.me.role==='admin'?`<button class="secondary" data-action="member-edit" data-id="${m.id}">Editar</button>`:''}</section>`).join('')}</div>`}
export function reports(ctx){return `<div class="metric-grid"><div class="metric"><span>Jornadas</span><b>${ctx.attendance?.length||0}</b></div><div class="metric"><span>Tareas terminadas</span><b>${(ctx.tasks||[]).filter(x=>x.status==='completed').length}</b></div><div class="metric"><span>Compras</span><b>${ctx.purchases?.length||0}</b></div><div class="metric"><span>Ausencias</span><b>${ctx.periods?.length||0}</b></div><div class="metric"><span>Pausas</span><b>${ctx.breaks?.length||0}</b></div><div class="metric"><span>Incidencias</span><b>${ctx.incidents?.length||0}</b></div></div><section class="card"><h3>Informe operativo</h3><p>Usa los filtros del navegador para imprimir o guardar este resumen. Los datos mostrados respetan los permisos del usuario conectado.</p></section>`}
export function audit(ctx){return `<section class="card table-card"><div class="table-scroll"><table><thead><tr><th>Fecha</th><th>Usuario</th><th>Acción</th><th>Detalle</th></tr></thead><tbody>${(ctx.audit||[]).map(x=>`<tr><td>${fmtDateTime(x.created_at)}</td><td>${e(person(x.member_id,ctx))}</td><td>${e(x.action)}</td><td>${e(x.detail||'')}</td></tr>`).join('')}</tbody></table></div></section>`}
export function maintenance(ctx){return `<div class="grid two"><section class="card"><div class="eyebrow">Backup portable</div><h3>Datos + archivos físicos</h3><p>Genera un paquete verificado con SHA-256. La restauración valida el paquete antes de modificar datos.</p><div class="actions"><button class="primary" data-action="backup-create">Descargar backup</button><label class="button-link secondary file-button">Restaurar<input hidden type="file" accept=".totusbackup,application/json" data-backup-input></label></div></section><section class="card danger-zone"><div class="eyebrow">Operación crítica</div><h3>Limpieza de datos operativos</h3><p>No elimina usuarios, tiendas ni configuración maestra.</p><button class="danger" data-action="reset-operational">Limpiar datos operativos</button></section></div><section class="card"><h3>Backups archivados</h3>${(ctx.backups||[]).map(b=>`<div class="list-row"><div><strong>${e(b.file_name)}</strong><span>${fmtDateTime(b.created_at)} · ${Math.round((b.file_size||0)/1024)} KB</span></div></div>`).join('')||empty('Sin backups archivados')}</section>`}
export function taskModal(ctx,t={}){return modal(t.id?'Editar tarea':'Nueva tarea',`<form id="taskForm" class="form-stack" data-id="${e(t.id||'')}">${field('Título','title',t.title||'','','required')}${textarea('Descripción','description',t.description||'')}<div class="form-grid two">${select('Asignar a','assigned_to',ctx.base.members.filter(x=>x.active).map(x=>[x.id,x.name]),t.assigned_to||ctx.me.id)}${select('Tienda','location_id',[['','Sin tienda'],...ctx.base.locations.map(x=>[x.id,x.name])],t.location_id||'')}${select('Prioridad','priority',[['low','Baja'],['normal','Normal'],['high','Alta'],['urgent','Urgente']],t.priority||'normal')}${field('Fecha límite','due_at',t.due_at?String(t.due_at).slice(0,16):'','datetime-local')}</div><label class="check"><input name="requires_evidence" type="checkbox" ${t.requires_evidence?'checked':''}> Foto/evidencia obligatoria al completar</label><button class="primary" type="submit">Guardar</button></form>`)}
export function attendanceModal(ctx){return modal('Iniciar jornada',`<form id="attendanceForm" class="form-stack">${select('Tienda','location_id',ctx.base.locations.map(x=>[x.id,x.name]),ctx.base.locations[0]?.id||'','','required')}<button class="primary" type="submit">Entrar</button></form>`)}
export function breakModal(){return modal('Pausa personal',`<form id="breakForm" class="form-stack">${select('Motivo','reason',[['cigarette','Cigarro'],['personal_call','Llamada personal'],['whatsapp','WhatsApp personal'],['mobile','Móvil personal'],['other','Otro']],'other')}${field('Tiempo estimado (min)','expected_minutes','5','number','min="0" max="480"')}<button class="primary" type="submit">Comenzar pausa</button></form>`)}
export function taskWorkModal(ctx,t){return modal(t.title,`<p>${e(t.description||'Sin descripción')}</p><div class="status-strip">${badge(priorityLabel(t.priority))}${badge(humanStatus(t.status))}</div><div class="actions" style="margin-top:14px">${ctx.panel?.timer?.task_id===t.id?'<button class="secondary" data-action="task-stop" data-id="'+ctx.panel.timer.id+'">Parar reloj</button>':'<button class="primary" data-action="task-start" data-id="'+t.id+'">Iniciar tarea</button>'}<button class="primary" data-action="task-complete" data-id="${t.id}" data-evidence="${t.requires_evidence?'1':'0'}">Completar</button></div>${t.requires_evidence?'<p class="inline-note">Esta tarea exige una foto o archivo antes de completarse.</p>':''}`)}
export function evidenceModal(taskId){return modal('Añadir evidencia',`<form id="evidenceForm" class="form-stack" data-task="${e(taskId)}"><label>Foto o archivo<input name="file" type="file" accept="image/*,.pdf" capture="environment" required></label>${textarea('Comentario','caption','')}<button class="primary" type="submit">Subir y completar</button></form>`)}
export function noticeModal(ctx){return modal('Nuevo aviso',`<form id="noticeForm" class="form-stack">${select('Destinatario','target_member_id',ctx.base.members.filter(x=>x.active).map(x=>[x.id,x.name]),'','','required')}${field('Título','title','','','required')}${textarea('Mensaje','message','','required')}<div class="form-grid two">${select('Tipo','type',[['info','Información'],['warning','Aviso'],['urgent','Urgente']],'warning')}${field('Caduca','expires_at','','datetime-local')}</div><label class="check"><input name="sound" type="checkbox" checked> Sonido</label><label class="check"><input name="requires_ack" type="checkbox"> Requiere confirmación</label><button class="primary" type="submit">Enviar aviso</button></form>`)}
export function purchaseModal(ctx){return modal('Nueva compra',`<form id="purchaseForm" class="form-stack">${select('Empleado','member_id',ctx.base.members.filter(x=>x.active).map(x=>[x.id,x.name]),ctx.me.id)}${select('Tienda','location_id',ctx.base.locations.map(x=>[x.id,x.name]),ctx.base.locations[0]?.id||'')}${select('Modalidad','mode',[['store','Producto existente en tienda (PVP -10%)'],['supplier','Pedido a proveedor (coste + IVA + RE + directos)']],'store')}${field('Producto','product_name','','','required')}<div class="form-grid two">${field('Cantidad','quantity','1','number','min="1" required')}${field('PVP tienda','pvp','','text','inputmode="decimal"')}${field('Coste neto proveedor','net_cost','','text','inputmode="decimal"')}${field('Costes directos/u','direct_costs','0','text','inputmode="decimal"')}</div>${textarea('Notas','notes','')}<button class="primary" type="submit">Guardar compra</button></form>`)}
export function leaveModal(){return modal('Solicitar ausencia',`<form id="leaveForm" class="form-stack">${select('Tipo','leave_type',[['vacation','Vacaciones'],['personal','Asunto propio'],['medical','Gestión médica'],['other','Otra ausencia']],'vacation')}<div class="form-grid two">${field('Desde','from_date','','date','required')}${field('Hasta','to_date','','date','required')}</div>${textarea('Motivo','reason','')}<button class="primary" type="submit">Enviar solicitud</button></form>`)}
export function giftModal(ctx){return modal('Regalar día',`<form id="giftForm" class="form-stack">${select('Empleado','member_id',ctx.base.members.filter(x=>x.active).map(x=>[x.id,x.name]),'')}${field('Fecha','date','','date','required')}${field('Motivo','reason','','','required')}<button class="primary" type="submit">Conceder día</button></form>`)}
export function holidayModal(ctx){return modal('Añadir festivo',`<form id="holidayForm" class="form-stack">${field('Nombre','name','','','required')}${field('Fecha','holiday_date','','date','required')}${select('Tienda','location_id',[['','General'],...ctx.base.locations.map(x=>[x.id,x.name])],'')}${select('Ámbito','scope',[['local','Local'],['regional','Autonómico'],['national','Nacional']],'local')}<button class="primary" type="submit">Guardar</button></form>`)}
export function fileModal(title,formId,ctx,employee=false){return modal(title,`<form id="${formId}" class="form-stack">${employee?select('Empleado','member_id',ctx.base.members.filter(x=>x.active).map(x=>[x.id,x.name]),ctx.me.id):''}${field('Título','title','','','required')}${field('Categoría','category','General')}${textarea('Notas','notes','')}<label>Archivo<input name="file" type="file" required></label><button class="primary" type="submit">Subir</button></form>`)}
export function questionModal(answer=false,q={}){return modal(answer?'Responder consulta':'Nueva consulta',answer?`<form id="answerForm" class="form-stack" data-id="${q.id}"><p>${e(q.question)}</p>${textarea('Respuesta','answer',q.answer||'','','required')}<button class="primary" type="submit">Guardar respuesta</button></form>`:`<form id="questionForm" class="form-stack">${textarea('Consulta','question','','required')}<button class="primary" type="submit">Enviar</button></form>`)}
export function incidentModal(ctx){return modal('Nueva incidencia',`<form id="incidentForm" class="form-stack">${select('Empleado','member_id',ctx.base.members.filter(x=>x.active).map(x=>[x.id,x.name]),ctx.me.id)}${select('Tipo','type',[['note','Nota'],['warning','Aviso'],['incident','Incidencia']],'note')}${field('Título','title','','','required')}${textarea('Descripción','description','','required')}<button class="primary" type="submit">Guardar</button></form>`)}
export function memberModal(ctx,m={}){const ids=ctx.base.memberLocations.filter(x=>x.member_id===m.id).map(x=>x.location_id);return modal(m.id?'Editar usuario':'Nuevo usuario',`<form id="memberForm" class="form-stack" data-id="${e(m.id||'')}">${field('Nombre','name',m.name||'','','required')}${field('Email','email',m.email||'','email','required')}${field('Teléfono','phone',m.phone||'','tel')}${select('Rol','role',[['admin','Administrador'],['gerente','Gerente'],['encargado','Encargado'],['tendero','Tendero'],['invitado','Invitado']],m.role||'tendero')}<fieldset><legend>Tiendas</legend>${ctx.base.locations.map(l=>`<label class="check"><input type="checkbox" name="locations" value="${l.id}" ${ids.includes(l.id)?'checked':''}> ${e(l.name)}</label>`).join('')}</fieldset><label class="check"><input type="checkbox" name="active" ${m.id?(m.active?'checked':''):'checked'}> Usuario activo</label><button class="primary" type="submit">Guardar usuario</button></form>`)}
export function productModal(ctx,p={}){return modal(p.id?'Editar producto':'Nuevo producto',`<form id="productForm" class="form-stack" data-id="${e(p.id||'')}"><div class="form-grid two">${field('SKU','sku',p.sku||'','','required')}${field('Nombre','name',p.name||'','','required')}${select('Marca','brand_id',[['','Sin marca'],...(ctx.brands||[]).map(x=>[x.id,x.name])],p.brand_id||'')}${select('Familia','family_id',[['','Sin familia'],...(ctx.families||[]).map(x=>[x.id,x.name])],p.family_id||'')}${field('PVP','pvp',p.pvp||'','text','inputmode="decimal"')}${select('Tipo','product_type',[['simple','Simple'],['variants','Variantes']],p.product_type||'simple')}</div>${textarea('Notas','notes',p.notes||'')}<button class="primary" type="submit">Guardar producto</button></form>`,true)}
export function consultationConvertModal(ctx,c){return modal('Convertir consulta en producto',`<form id="consultationConvertForm" data-id="${c.id}" class="form-stack">${field('SKU','sku','','','required')}${select('Marca','brand_id',[['','Sin marca'],...(ctx.brands||[]).map(x=>[x.id,x.name])],'')}${select('Familia','family_id',[['','Sin familia'],...(ctx.families||[]).map(x=>[x.id,x.name])],'')}${select('Proveedor','provider_id',[['','Sin proveedor'],...(ctx.providers||[]).map(x=>[x.id,x.name])],'')}<button class="primary" type="submit">Crear producto</button></form>`)}
export function resetModal(){return modal('Confirmar limpieza',`<form id="resetForm" class="form-stack"><p>Se eliminarán tareas, fichajes, pausas, compras, avisos, consultas operativas e incidencias. No se borran usuarios ni configuración maestra.</p>${field('Motivo','reason','','','required minlength="3"')}<button class="danger" type="submit">Confirmar limpieza</button></form>`)}
